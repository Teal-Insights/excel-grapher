schema_version: "1.4.0"
workbook: matrix_compute_inputs.xlsx
series:
  - id: macro_matrix
    sheet: Inputs
    data_range: Inputs!B3:D5
    layout: matrix
    editable: false
    output:
      compute:
        name: compute_macro_matrix
    structure:
      measure:
        concept: OBS_VALUE
        dtype: float
        bind:
          kind: data_cell
          read: float
      dimensions:
        - concept: INDICATOR
          role: key
          scope: cell
          bind:
            kind: row_label
            label_column: A
            read: string
            normalize: strip
        - concept: TIME_PERIOD
          role: key
          scope: cell
          bind:
            kind: column_header
            header_row: 2
            read: int
    key:
      - INDICATOR
      - TIME_PERIOD
    notes: Matrix compute fixture; workbook cells use =value*2 formulas.
